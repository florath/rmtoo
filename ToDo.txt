new features
o use git for all requirement read in
  - add paeameter --base which gets a version string which
    specifies on which version to work
  - possibility to get minimal history statistics
  - possibility to do effort vorhersage
  - gives the base for traceability (think about!)
o change text output to base on a topic structure
o Makefile dependencies must be done directly in the python tool
  itself (This is the one which knows about.)
o Every topic should go to a separate output file?

