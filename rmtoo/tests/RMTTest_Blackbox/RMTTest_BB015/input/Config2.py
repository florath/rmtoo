class Config:
    stakeholders = ["development", "management", "users", "customers"]

    inventors = ["flonatel", ]

    reqs_spec = {
        "directory": '/tmp/ignore/me',
        "commit_interval": ['FILES', 'FILES'],
        "default_language": "kl_EL",
        "dependency_notation": set(["Depends on", "Solved by"])
        }
