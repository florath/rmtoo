import os

class Config:
    stakeholders = ["development", "management", "users", "customers"]

    inventors = ["flonatel", ]

    reqs_spec = {
        "dependency_notation": set(["Depends on", "Solved by"])
        }
