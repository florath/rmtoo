This directory contains all the blackbox tests where rmtoo is called
directly (and not parts of it) simulating a real call.

All input files are prepared for the appropriate test case.

All output files (including the stdout and stderr) are checked
afterwards.

