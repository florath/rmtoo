language: python

git:
  depth: 5000

dist:  bionic

python:
  - "3.6"
  - "3.7"
  - "3.8"

# Required to run the respective python env in tox
install: pip install tox-travis

# command to run tests
script:
  - tox
  - tox -e pep8

